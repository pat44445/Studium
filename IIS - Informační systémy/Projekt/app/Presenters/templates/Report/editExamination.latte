{block content}
    <div class="list-content">
        <div class="list-header">
            <h2>Upravit lékařskou zprávu</h2>
            <a class="button-edit" type="create" n:href="Examination:detail examinationId=>$examinationId">zpět</a>
        </div>
        <hr>
        <div>
            <p style="color: red">Položky označené * jsou povinné</p>
            {form form}
                <table>
                <tr n:foreach="$form->controls as $name => $field">
                    {if $name == "subject" or $name == "text"}
                        <th>{label $name /} *<th>
                    {else}
                        <th>{label $name /}<th>
                    {/if}
                    <td>{input $name}</td>
                    <td n:ifcontent>{inputError $name}</td>
                </tr>
                </table>
            {/form}
        </div>
    </div>
{/block}